[pytest]
testpaths = tests
asyncio_mode = auto
# pytest-homeassistant-custom-component ships the `hass` fixture and the
# HA event-loop plumbing; enable it as a plugin explicitly so a bare
# `pytest` invocation from the repo root picks it up.
addopts = -p no:cacheprovider
